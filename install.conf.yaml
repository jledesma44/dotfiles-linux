- defaults:
    link:
      relink: true

- clean: ["~"]

- create:
    - ~/Development

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config/bat:
    ~/.config/nvim:
    ~/.config/kitty:
    ~/Bin:

- shell:
    - command: ./setup_ohmyzsh.zsh
      stdout: true
      stderr: true
    - command: ./setup_tpm.zsh
      stdout: true
      stderr: true
