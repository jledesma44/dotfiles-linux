- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.zshenv: 
    ~/.gitconfig: 
    ~/.tmux.conf: 
    ~/.config/karabiner:

#-Comment test    
      
# - create:
    # - ~/Work

# - shell:
   # - 
    # command: ./setup_homebrew.zsh
    # stdout: true
    # stderr: true
