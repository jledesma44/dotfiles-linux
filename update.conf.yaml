- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config/bat:
    ~/.config/nvim:
    ~/.config/kitty:
    ~/.config/kmonad:
    ~/.config/hypr:
    ~/.config/waybar:
    ~/.local/bin:

- shell:
    - command: ./2.Packages-list/packages_install_update.zsh
      stdout: true
      stderr: true
      stdin: true
